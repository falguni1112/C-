#include<iostream>
using namespace std;

int main(){
    int x;
    // ask the user to enter a number
    cout <<"Enter a number to be displayed";
    // accept the input from user into a predefined variable
    cin >> x;
    // diplay the variable back to the user
    cout << "The double of given number is: "<< (2 * x);

    return 0;
}